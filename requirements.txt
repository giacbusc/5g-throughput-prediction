# Core data science
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Machine Learning
scikit-learn>=1.3.0
tensorflow>=2.15.0      # Neural Network (Keras). Pre-installed on Google Colab.

# Visualization
matplotlib>=3.7.0

# Data download / model persistence
gdown>=5.0.0            # download the dataset zip from the shared Google Drive folder
joblib>=1.3.0           # save/load scikit-learn models & scalers

# Jupyter (local runs only; Colab provides its own)
jupyter>=1.0.0
ipykernel>=6.0.0
