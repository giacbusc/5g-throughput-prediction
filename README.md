# 5G Throughput Prediction with Transfer Learning

This repository contains **Project #8** developed for the **Network Data Analysis Laboratory (2025-2026)** course at **Politecnico di Milano**.

**Team:** Giacomo Buscaglia, Zarlene de Mesa, Davide Roccuzzo

---

## 🎯 Objective

> Can we predict the user throughput given user terminal and radio unit features in a dense 5G deployment?

The project is split into two main components:

| Task | Points | Description |
|------|--------|-------------|
| **Regression** | 12 pts | Throughput regression on ACC Arena users using **Neural Network** and **Random Forest**, augmented with features of the **X closest users** |
| **Transfer Learning** | 3 pts | Fine-tune an ACC Arena model on a limited Salt&Tar training set; compare against training from scratch |

---

## 📊 Dataset

The project uses the *5G High Density Demand (HDD) Dataset in Liverpool City Region, UK* [(Maheshwari et al., 2025)](https://doi.org/10.1038/s41597-025-04580-5):

| Venue | Users | Sampling rate | Samples/user |
|-------|-------|---------------|--------------|
| **ACC Arena** | 12,000 | 1 sample / 3 s | ~10,000 |
| **Salt & Tar** | 3,000 | 1 sample / s | ~10,000 |

**Features per sample:** `timestamp, user_id, x, y, z, traffic_type, ru_id, sinr_dl, sinr_ul, throughput, prb, bler`

**Traffic type classes:** 0=off, 1=idle, 2=constant_rate, 3=video, 4=gaming, 5=http

---

## 🏗️ Project Structure

```
5g-throughput-prediction/
├── data/
│   ├── raw/                  # ⚠️ Not committed — place dataset CSVs here
│   └── processed/            # Auto-generated by notebook 02
├── notebooks/
│   ├── 01_eda.ipynb                  # Exploratory Data Analysis
│   ├── 02_preprocessing.ipynb        # Cleaning, scaling, splitting
│   ├── 03_feature_engineering.ipynb  # X closest-user features
│   ├── 04_model_training.ipynb       # NN + RF + hyperparameter tuning
│   ├── 05_evaluation.ipynb           # Metrics, comparison plots
│   └── 06_transfer_learning.ipynb    # ⭐ Advanced: TL ACC Arena → Salt&Tar
├── src/
│   ├── data/
│   │   ├── loader.py         # Load raw CSVs, normalise columns
│   │   └── preprocessor.py   # Scale, encode, split
│   ├── features/
│   │   └── closest_users.py  # KD-tree based X-nearest-neighbour features
│   ├── models/
│   │   ├── neural_network.py     # MLP (PyTorch) with early stopping
│   │   ├── random_forest.py      # RF (scikit-learn) + Optuna tuning
│   │   └── transfer_learning.py  # Fine-tuning & scratch baseline
│   └── utils/
│       ├── metrics.py        # MSE, RMSE, MAE, R², MAPE, timing
│       └── visualization.py  # Reusable matplotlib/seaborn helpers
├── results/
│   ├── figures/              # Auto-generated plots
│   └── models/               # Saved model checkpoints
├── requirements.txt
└── CLAUDE.md                 # Developer guide for Claude Code
```

---

## 🔬 Methodology

### Step 1 — EDA (`01_eda.ipynb`)
- Distribution plots, correlation heatmaps, spatial throughput maps
- Traffic type analysis, outlier detection

### Step 2 — Preprocessing (`02_preprocessing.ipynb`)
- Drop NaN, filter impossible values (BLER ∉ [0,1], throughput ≤ 0)
- One-hot encode `traffic_type` (6 classes)
- StandardScale numeric features
- 75 / 10 / 15 train / val / test split

### Step 3 — Feature Engineering (`03_feature_engineering.ipynb`)
- For each user sample, compute 3D Euclidean distance to all other users at the same timestamp
- Append features of the X nearest neighbours (SINR DL/UL, PRB, BLER, traffic type, throughput)
- Experiment with **X ∈ {1, 3, 5, 10}**

### Step 4 — Model Training (`04_model_training.ipynb`)
- **Neural Network (MLP):** PyTorch, configurable depth/width, BatchNorm, Dropout, early stopping; hyperparameter tuning with **Optuna** (30 trials)
- **Random Forest:** scikit-learn, hyperparameter tuning with **Optuna + 5-fold CV** (50 trials)

### Step 5 — Evaluation (`05_evaluation.ipynb`)
- Metrics: **MSE, RMSE, MAE, R², MAPE, training time, inference time**
- Predicted vs. true scatter, residual plots, feature importance
- Sensitivity analysis: metric vs. X

### Step 6 — Transfer Learning (`06_transfer_learning.ipynb`)
- **Fine-tuning:** Load ACC Arena weights → freeze early layers → fine-tune on limited Salt&Tar data
- **Scratch baseline:** Same architecture, random init, same limited Salt&Tar data
- Comparison as a function of training set size

---

## 🚀 Getting Started

```bash
# 1. Clone the repository
git clone https://github.com/giacbusc/5g-throughput-prediction.git
cd 5g-throughput-prediction

# 2. Install dependencies
pip install -r requirements.txt

# 3. Place dataset files
cp /path/to/acc_arena.csv  data/raw/acc_arena.csv
cp /path/to/salt_tar.csv   data/raw/salt_tar.csv

# 4. Run notebooks in order
jupyter notebook notebooks/
```

---

## 📈 Results Summary

> *(To be filled after running all notebooks)*

| Model | X | RMSE (Mbps) | MAE (Mbps) | R² | Train time (s) |
|-------|---|-------------|------------|-----|----------------|
| RF    | 0 |             |            |     |                |
| RF    | best |          |            |     |                |
| MLP   | 0 |             |            |     |                |
| MLP   | best |          |            |     |                |

---

## 📚 References

1. Maheshwari, M. K., et al. *"5G High Density Demand Dataset in Liverpool City Region, UK."* Scientific Data (2025).
2. Maheshwari, M. K., et al. *"5G High Density Demand (HDD) Dataset in Liverpool City Region, UK (Supplement)."* (2025).
